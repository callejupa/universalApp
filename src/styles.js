import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    content: {
        //paddingTop: 40,
        flex: 1,
        flexDirection: 'row'
        }
})

export default styles
